version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to prebuild stage......
  build:
    commands:
      - echo Build started on `date`
      - echo buildstage.....
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
    files: cft.yml   
